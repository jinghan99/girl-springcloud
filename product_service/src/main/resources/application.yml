
server:
   port: 8771
#   指定服务注册中心
eureka:
  instance:
      hostname: localhost
  client:
    serviceUrl:
        defaultZone: http://${eureka.instance.hostname}:${server.port}/api/
spring:
  application:
    name: product-service